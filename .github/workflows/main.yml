name: Create Windows RDP with Tailscale (fixed installer)

on:
  workflow_dispatch:

jobs:
  setup-rdp:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Tailscale + RDP
        shell: pwsh
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
          NEW_RDP_USER: ${{ secrets.NEW_RDP_USER || 'rdpuser' }}
          NEW_RDP_PASS: ${{ secrets.NEW_RDP_PASS || 'ChangeMe@1234' }}
        run: |
          # --- Check Auth Key ---
          if (-not $env:TAILSCALE_AUTHKEY -or $env:TAILSCALE_AUTHKEY.Trim() -eq "") {
            Write-Error "❌ Missing secret: TAILSCALE_AUTHKEY"
            exit 1
          }

          Write-Host "⬇ Downloading Tailscale installer (EXE)..."
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe"
          $out = "$env:TEMP\tailscale-setup.exe"
          Invoke-WebRequest -Uri $url -OutFile $out -UseBasicParsing

          Write-Host "📦 Installing Tailscale silently..."
          Start-Process -FilePath $out -ArgumentList "/S" -Wait -NoNewWindow
          Start-Sleep -Seconds 6

          # Find tailscale.exe
          $tailscaleExe = (Get-ChildItem "C:\Program Files*" -Recurse -ErrorAction SilentlyContinue |
            Where-Object { $_.Name -eq "tailscale.exe" } |
            Select-Object -First 1).FullName

          if (-not $tailscaleExe) {
            Write-Error "❌ Could not find tailscale.exe after install!"
            exit 1
          }

          Write-Host "✅ Found Tailscale at: $tailscaleExe"
          Write-Host "🚀 Logging into Tailscale..."
          & $tailscaleExe up --authkey $env:TAILSCALE_AUTHKEY --accept-routes --hostname "github-rdp" | Write-Host
          Start-Sleep -Seconds 3

          Write-Host "👤 Creating RDP user..."
          $user = $env:NEW_RDP_USER
          $pass = $env:NEW_RDP_PASS
          $secure = ConvertTo-SecureString $pass -AsPlainText -Force
          if (-not (Get-LocalUser -Name $user -ErrorAction SilentlyContinue)) {
            New-LocalUser -Name $user -Password $secure -FullName "GitHub RDP User" -Description "Created by Workflow"
            Add-LocalGroupMember -Group "Administrators" -Member $user
          } else {
            Set-LocalUser -Name $user -Password $secure
          }

          Write-Host "🔓 Enabling Remote Desktop..."
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name 'fDenyTSConnections' -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

          Write-Host "🌐 Checking Tailscale IP..."
          & $tailscaleExe ip

          Write-Host "`n✅ DONE! Connect to your RDP via the above Tailscale IP."
          Write-Host "Username: $user"
          Write-Host "Password: $pass"
